<?xml version="1.0" encoding="utf-8"?>
<!--
   Android 12 et plus.

   Cloud : rien. Une section `cloud-backup` sans `include` sauvegarde tout, donc
   chaque domaine est exclu en entier. La politique de confidentialité promet que
   la progression, le carnet et les préférences ne quittent jamais l'appareil.

   Transfert d'appareil : le carnet suit le joueur quand il change de téléphone,
   par câble ou en Wi-Fi direct, sans passer par un serveur. Le compteur de frappe
   et sa référence (creole_dict_with_usage.json, carnet_vu.json) restent exclus :
   ce qui a été tapé ne part nulle part. BackupRulesTest vérifie les deux sections.
-->
<data-extraction-rules xmlns:tools="http://schemas.android.com/tools">
    <cloud-backup>
        <exclude domain="root" path="."/>
        <exclude domain="file" path="."/>
        <exclude domain="database" path="."/>
        <exclude domain="sharedpref" path="."/>
        <exclude domain="external" path="."/>
    </cloud-backup>

    <device-transfer>
        <include domain="sharedpref" path="."/>
        <exclude domain="sharedpref" path="device.xml"/>
        <exclude domain="file" path="creole_dict_with_usage.json"
            tools:ignore="FullBackupContent"/>
        <exclude domain="file" path="carnet_vu.json"
            tools:ignore="FullBackupContent"/>
    </device-transfer>
</data-extraction-rules>
